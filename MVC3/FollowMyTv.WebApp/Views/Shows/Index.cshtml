@model IEnumerable<FollowMyTv.DomainLayer.Show>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section IncludeScripts {
    <script type="text/javascript" src="@Url.Content( "~/Scripts/InfiniteScroll.js" )"></script>
}
<h2>
    Index</h2>
<p>
    @Html.ActionLink( "Create New", "Create" )
    @Html.ActionLink( "Paged Version", "Index" )
</p>
<div id="paging_content">
    @Html.Partial( "_IndexPaged", Model )
</div>
@*<table>
    <tr>
        <th>
            Name
        </th>
        <th>
            Description
        </th>
        <th>
        </th>
    </tr>
    @foreach ( var item in Model )
    {
        <tr>
            <td>
                @Html.DisplayFor( modelItem => item.Name )
            </td>
            <td>
                @Html.DisplayFor( modelItem => item.Description )
            </td>
            <td>
                @Html.ActionLink( "Edit", "Edit", new { id = item.Id } )
                @Html.ActionLink( "Details", "Details", new { id = item.Id } )
                @Html.ActionLink( "Delete", "Delete", new { id = item.Id } )
                @Html.ActionLink( "Seasons", "List", new { show = item.Id } )
            </td>
        </tr>
    }
</table>*@